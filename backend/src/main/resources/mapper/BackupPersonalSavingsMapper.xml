<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lx.mapper.BackupPersonalSavingsMapper">

    <!-- 批量插入个人存钱计划备份数据 -->
    <insert id="insertBatch" parameterType="list">
        INSERT INTO backup_personal_savings (
        id, backup_id, original_id, user_id, name, type,
        target_amount, current_amount, deadline, status, color, icon, note, deleted, created_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id}, #{item.backupId}, #{item.originalId}, #{item.userId},
            #{item.name}, #{item.type}, #{item.targetAmount}, #{item.currentAmount},
            #{item.deadline}, #{item.status}, #{item.color}, #{item.icon},
            #{item.note}, #{item.deleted}, #{item.createdAt}
            )
        </foreach>
    </insert>

</mapper>