<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lx.mapper.BackupPurchaseHistoryMapper">

    <!-- 批量插入采购历史备份数据 -->
    <insert id="insertBatch" parameterType="list">
        INSERT INTO backup_purchase_history (
        id, backup_id, original_id, user_id, supplier_id, order_no,
        product_id, product_name, quantity, unit, price, total_amount,
        purchase_date, note, created_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id}, #{item.backupId}, #{item.originalId}, #{item.userId},
            #{item.supplierId}, #{item.orderNo}, #{item.productId}, #{item.productName},
            #{item.quantity}, #{item.unit}, #{item.price}, #{item.totalAmount},
            #{item.purchaseDate}, #{item.note}, #{item.createdAt}
            )
        </foreach>
    </insert>

</mapper>