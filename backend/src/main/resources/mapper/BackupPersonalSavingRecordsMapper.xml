<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lx.mapper.BackupPersonalSavingRecordsMapper">

    <!-- 批量插入个人存钱记录备份数据 -->
    <insert id="insertBatch" parameterType="list">
        INSERT INTO backup_personal_saving_records (
        id, backup_id, plan_id, amount,deposit_time,
        before_amount, after_amount, note, created_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id}, #{item.backupId},
            #{item.planId}, #{item.amount}, #{item.depositTime},
            #{item.beforeAmount}, #{item.afterAmount}, #{item.note},
            #{item.createdAt}
            )
        </foreach>
    </insert>

</mapper>