<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lx.mapper.BackupProductsMapper">

    <!-- 批量插入商品备份数据 -->
    <insert id="insertBatch" parameterType="list">
        INSERT INTO backup_products (
        id, backup_id, original_id, user_id, name, category, unit,
        default_price, default_purchase_price, note, created_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id}, #{item.backupId}, #{item.originalId}, #{item.userId},
            #{item.name}, #{item.category}, #{item.unit},
            #{item.defaultPrice}, #{item.defaultPurchasePrice}, #{item.note}, #{item.createdAt}
            )
        </foreach>
    </insert>

</mapper>