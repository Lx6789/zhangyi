<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lx.mapper.BackupProductCategoriesMapper">

    <!-- 批量插入商品分类备份数据 -->
    <insert id="insertBatch" parameterType="list">
        INSERT INTO backup_product_categories (
        id, backup_id, original_id, user_id, name, icon, sort_order, is_default, created_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id}, #{item.backupId}, #{item.originalId}, #{item.userId},
            #{item.name}, #{item.icon}, #{item.sortOrder}, #{item.isDefault}, #{item.createdAt}
            )
        </foreach>
    </insert>

</mapper>