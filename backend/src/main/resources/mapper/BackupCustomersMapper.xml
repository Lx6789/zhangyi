<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lx.mapper.BackupCustomersMapper">

    <!-- 批量插入客户备份数据 -->
    <insert id="insertBatch" parameterType="list">
        INSERT INTO backup_customers (
        id, backup_id, original_id, user_id, name, type, phone, address,
        credit_info, stats, note, created_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id}, #{item.backupId}, #{item.originalId}, #{item.userId},
            #{item.name}, #{item.type}, #{item.phone}, #{item.address},
            #{item.creditInfo}, #{item.stats}, #{item.note}, #{item.createdAt}
            )
        </foreach>
    </insert>

</mapper>