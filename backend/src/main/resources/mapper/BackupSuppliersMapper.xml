<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lx.mapper.BackupSuppliersMapper">

    <!-- 批量插入供应商备份数据 -->
    <insert id="insertBatch" parameterType="list">
        INSERT INTO backup_suppliers (
        id, backup_id, original_id, user_id, name, contact_person,
        phone, email, category, address, payment_terms, payment_days, note, created_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id}, #{item.backupId}, #{item.originalId}, #{item.userId},
            #{item.name}, #{item.contactPerson}, #{item.phone}, #{item.email},
            #{item.category}, #{item.address}, #{item.paymentTerms},
            #{item.paymentDays}, #{item.note}, #{item.createdAt}
            )
        </foreach>
    </insert>

</mapper>